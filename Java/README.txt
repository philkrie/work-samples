Dynamic Split:

A dynamic programming solution to a problem: can this string be broken up into a sentence of english words? Determines the solution in two ways. The first is an iterative bottom up approach, while the other is a memoized approach.

Longest/Shortest/Number of paths in DAG:

A program that uses a modified version of Dijkstra’s and a BFS to determine the longest path, the shortest path, and the number of paths from the start to the end of a directed acyclic graph.